pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                
                echo 'build'
                sh '''
                    echo "here is the new jenkinsfile" 
                    echo "help me learn" 
                '''
            }
        }
    }
}